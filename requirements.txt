# Python dependencies for AI Demo gRPC server
grpcio>=1.76.0
grpcio-tools>=1.76.0
protobuf>=6.33.0

# AI model – local Hugging Face (Qwen3). torch is installed in Dockerfile.dev (CPU wheels only).
transformers>=4.51.0,<5.0.0
accelerate>=0.25.0

# Linting (dev)
ruff>=0.8.0

# Testing dependencies
pytest==8.3.4
grpcio-testing>=1.76.0
