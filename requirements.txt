# Python dependencies for AI Demo gRPC server
grpcio>=1.80.0
grpcio-tools>=1.80.0
protobuf>=6.33.0,<7

# AI generation uses Ollama over HTTP; no local PyTorch/Transformers runtime in this container.

# Linting (dev)
ruff>=0.15.0

# Testing dependencies
pytest>=9.0.0
grpcio-testing>=1.80.0
